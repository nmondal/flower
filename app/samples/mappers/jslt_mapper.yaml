
nested_mapper:
  _each: "@//products"
  id : "#id"
  networkId : "#../../id"
  networkDescription : "#../../description"
  productIdentity : "#../id"
  productCatTypeName : "#../description"
  minAmount : "#minAmount"

base_mapper:
  a : "#x"
  b : "#y"
  t : "$.x + $.y"

mapper_redirect:
  _each: "#."
  "*" : "&base_mapper"


